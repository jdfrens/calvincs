<li id="event_<%= event.id %>">
  <strong><%= event.descriptor.capitalize %> <%= timing %>!</strong>
  <span><%= event.title %></span>
  <a class="more" href="/event#event-<%= event.id %>">more...</a>
</li>
