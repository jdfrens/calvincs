<h1>Change Password for <%= @user.full_name %></h1>

<p>Passwords are sent in plaintext, <strong>so pick a simple password that you don't use anywhere else</strong>.</p>

<% form_for @user do |form| -%>
    <%= form.error_messages %>
    <table>
      <tr>
        <td><%= form.label :password %></td>
        <td><%= form.password_field :password %></td>
      </tr>
      <tr>
        <td><%= form.label :password_confirmation %></td>
        <td><%= form.password_field :password_confirmation %></td>
      </tr>
      <tr>
        <td></td>
        <td><%= form.submit "Update" %></td>
      </tr>
    </table>
<% end -%>
