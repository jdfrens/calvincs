<h1>Log In</h1>

<%= form_for :user, :url => login_path do |form| %>
  <p>name: <%= form.text_field :username %></p>
  <p>password: <%= form.password_field :password %></p>
  <%= submit_tag "Login" %>
<% end %>
