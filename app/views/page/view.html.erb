<h1>
  <% if @page.subpage? -%>
      {{ A SUBPAGE HAS NO TITLE }}
  <% else -%>
      <%= h(@page.title) %>
  <% end -%>
</h1>

<div id="page_content">
  <%= render :partial => "image" %>
  <%= textilize @page.content %>
</div>
