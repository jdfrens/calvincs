<% form_for @newsitem do |form| %>
    <table summary="news item form">
      <tr>
        <td> <%= form.label :headline, "Headline:" %> </td>
        <td> <%= form.text_field :headline, :size => 40 %> </td>
      </tr>
      <tr>
        <td> Teaser:</td>
        <td> <%= form.text_field :teaser, :size => 65 %> </td>
      </tr>
      <tr>
        <td> Content:</td>
        <td> <%= form.text_area :content, :rows => 5, :cols => 65 %> </td>
      </tr>
      <tr>
        <td> <%= form.label :goes_live_at, "Goes live:" %> </td>
        <td> <%= form.date_select :goes_live_at %> </td>
      </tr>
      <tr>
        <td> Expires:</td>
        <td> <%= form.date_select :expires_at %> </td>
      </tr>
      <tr>
        <td></td>
        <td> <%= submit_tag %> </td>
      </tr>
    </table>
<% end %>

<p>
  The <strong>teaser</strong> should be kept very short; it is used in summaries (as in the display on the home page).
  The content can (and should) use Textile formatting.
</p>
