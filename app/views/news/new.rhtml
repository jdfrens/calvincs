<h1>Create News Item</h1>

<% if flash[:error] -%>
  <div id="error">
    <%= error_messages_for(:news_item) %>
  </div>
<% end -%>

<% form_for :news_item, :url => { :action => :save } do |form| %>
  <table summary="news item form">
    <tr>
      <td> Headline: </td>
      <td> <%= form.text_field :headline, :size => 40 %> </td>
    </tr>
    <tr>
      <td> Teaser: </td>
      <td> <%= form.text_field :teaser, :size => 80 %> </td>
    </tr>
    <tr>
      <td> Content: </td>
      <td> <%= form.text_area :content, :rows => 5, :cols => 80 %> </td>
    </tr>
    <tr>
      <td> Expires: </td>
      <td> <%= form.date_select :expires_at %> </td>
    </tr>
    <tr>
      <td> </td>
      <td> <%= submit_tag %> </td>
    </tr>
  </table>
<% end %>

<p>The <strong>teaser</strong> should be kept very short; it is used in summaries (as in the display on the home page).  The content can (and should) use Textile formatting.</p>
